name: student_assistant_app
description: A Student Assistant Application
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  file_picker: ^8.0.0
  flutter:
    sdk: flutter
  
  image_picker: ^1.2.2
  provider: ^6.1.0
  supabase_flutter: ^2.12.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true